{
  "name": "toonstream-scraper",
  "version": "3.0.0",
  "description": "Vercel edge scraper for toonstream — zero external dependencies",
  "main": "api/index.js",
  "type": "module",
  "scripts": {
    "dev":    "vercel dev",
    "deploy": "vercel"
  },
  "engines": {
    "node": ">=18.x"
  }
}
